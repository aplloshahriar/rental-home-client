import React from 'react';

const Button = () => {
    return (
        <div>
            <button onClick={() => handleAddToWatch(home)} className='btn btn-circle btn-outline'><Avatar src="/broken-image.jpg" /></button>
        </div>
    );
};

export default Button;